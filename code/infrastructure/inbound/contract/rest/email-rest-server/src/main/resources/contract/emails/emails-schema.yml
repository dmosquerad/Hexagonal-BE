schemas:
  emailBlockRules:
    type: object
    properties:
      email:
        type: array
        items:
          type: string
          format: email
      host:
        type: array
        items:
          type: string
      tld:
        type: array
        items:
          type: string
      domain:
        type: array
        items:
          type: string
      username:
        type: array
        items:
          type: string
