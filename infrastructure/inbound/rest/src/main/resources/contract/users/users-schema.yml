schemas:
  user:
    type: object
    required:
      - email
      - name
    properties:
      email:
        type: string
        format: email
      name:
        type: string
  userCreate:
    allOf:
      - $ref: './users-schema.yml#/schemas/user'
  userRead:
    allOf:
      - $ref: './users-schema.yml#/schemas/user'
      - type: object
        required:
          - userId
        properties:
          userId:
            type: string
            format: uuid